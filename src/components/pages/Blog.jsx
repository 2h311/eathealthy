import React from "react";
import useDocumentTitle from "../dry/useDocumentTitle";

const Blog = () => {
  useDocumentTitle("EatHealthy — Blog Page");
  return <div>blog</div>;
};

export default Blog;
